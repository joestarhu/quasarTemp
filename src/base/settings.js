const ConfigDict = {
    // 本地化语言(从localsession中获取)
    locale: "user-locale",
    // 外观颜色(从localsession中获取)
    appearance: "auto",
    // 默认分页大小
    pageSize: 10,
    // 分页的可选项
    rowsPerPageOptions: [10, 20, 30, 50],
    // 防抖默认时间:毫秒
    debounce: 500,
}


export { ConfigDict }